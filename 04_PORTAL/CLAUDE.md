@AGENTS.md

## Next.js のインポートパス制限【最重要】
- `04_PORTAL` (Next.js) 内において、絶対パスエイリアス（`@/`）は使用を一切禁止します。
- Vercel 等のデプロイ環境での `module-not-found` エラーを完全に防ぐため、常に相対パス（`../../` など）でインポートを記述してください。

## 新規Supabaseテーブル／APIルートを追加するとき
過去に何度も同じ抜け漏れ（RLSを`USING (true)`のまま公開し、対応するAPIルートにも認証チェックを付け忘れる）が発生しています（migrations 38/39/43参照）。新しいテーブルや`/api/**`ルートを追加する際は `supabase-table-security` スキルのチェックリストに従ってください。
