name = "ktm-os-worker"
main = "src/index.js"
compatibility_date = "2024-04-17"

# [vars]
# DISCORD_PUBLIC_KEY = "..."
# DISCORD_TOKEN = "..."
# LOCAL_API_URL = "..."
# PORTAL_API_URL = "https://ktm-portal.vercel.app"

[triggers]
# 既存: 金土20時直前通知 / 土21時イベント作成 / 水21時事前告知
# 追加: 10分ごとに募集の開始リマインドを走査(D1)
crons = [ "0 11 * * 5,6", "0 12 * * 6", "0 12 * * 3", "*/10 * * * *" ]
