# Antigravity Secrets
**/client_secrets.json
**/token.pickle
**/.env

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/

# IDE
.vscode/

# Temporary / Debug
debug_*.py
.DS_Store
node_modules

Dropbox/
.obsidian/
00_LOGS/
scratch/
.gemini/
**/chroma/
*.mp3
*.mp4
*.m4a
*.json
!package.json
!package-lock.json
!**/MatchupMemo/*.json
!**/vercel.json
!04_PORTAL/tsconfig.json
# src/shared/ 配下はソースコードが import するデータファイル（例: ktm_tiers.json）。
# これを無視すると手元では動くのにVercel/Botのクリーンビルドで Module not found になる。
!**/src/shared/*.json
# 辞典一括更新のキュー進捗(champ_db_bulk_updater.py)。champ-dict-update.ymlが
# 実行後にgit commitで永続化する設計だが、*.jsonに巻き込まれてgit add自体が
# 常に黙って失敗しており、GitHub Actions実行のたびにキューが失われ、
# 毎回全チャンピオンを最初からやり直していた(2026-08-12発覚)。
!02_FACTORY/_LOL/champion_update_queue.json
# Supabase Edge Functions (memory-encoder/stats-collector)のimport map。tsconfig.jsonと同じ理由
# (2026-08-04発覚)でこれも*.jsonに巻き込まれ未追跡だった。本番デプロイ済み関数が参照する設定のため必須。
!supabase/functions/*/deno.json
99_ARCHIVE/
02_FACTORY/INFRA/outbox/
02_FACTORY/INFRA/cache/

.agent/playwright_data/
.vercel
**/.env.bak

# Generated products & temporary assets
02_FACTORY/note_drafts/
02_FACTORY/sns_assets/
02_FACTORY/sns_promotions/
02_FACTORY/PRODUCTS/

# Wrangler / Cloudflare temporary files
.wrangler/
**/tmp/bundle-*/
**/tmp/dev-*/

.vercel
.env*

# Cloudflare Wrangler local variables
**/.dev.vars

# ローカルSQLiteの実行時DB（頻繁に更新されGit肥大化の原因になるため追跡しない）
*.db

# PyInstallerのビルド成果物（SovereignMind.spec から再生成可能）
03_SYSTEMS/v2_CORE/_LOL/mental_checker/build/
03_SYSTEMS/v2_CORE/_LOL/mental_checker/dist/
